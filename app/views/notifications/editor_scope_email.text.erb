INSTRUCTIONS

Please review the recently-flagged submissions below and vote on the <%= Rails.application.settings["abbreviation"] %> model page whether you believe the submission is in scope, or out of scope.

<% if @query_scope_issues.any? %>
  <% @query_scope_issues.each do |issue| %>
    <% unless Vote.has_vote_for?(issue.model, @editor) %>
      <% if issue.pre_review? %>

<%= issue.title %> (<%= issue.model.created_at.strftime('%F') %>)
  
<%= issue.model.body %>
  
VOTE ON SCOPE HERE: <%= Rails.application.settings["url"] %>/models/<%= issue.model.sha %>
Review issue: https://github.com/<%= Rails.application.settings["reviews"] %>/issues/<%= issue.number %>
Software repository: <%= issue.model.repository_url %>

–––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––––------------
      <% end %>
    <% end %>
  <% end %>
<% end %>